# 安全策略（Security Policy）

PC-Cleaner 是一个**会删除文件**的工具，安全边界是它的核心设计目标。本文件说明支持范围、
安全模型、已知边界与漏洞上报方式。

## 支持的版本

| 版本 | 安全更新 |
|---|---|
| 0.9.x（当前） | ✅ |
| < 0.9 | ❌（请升级，0.9.0 之前的版本缺少重解析点防护与删除前二次防御） |
## 安全模型（摘要）

完整说明见 README 的「安全保证」章节。核心约定：

1. **不确认不删除**：默认先扫描预览，人工确认后才执行；`--dry-run` 永不删除。
2. **默认进回收站**：`send2trash` 是必需依赖，删除可恢复；进回收站失败时默认**保留原文件**。
3. **危险操作闸门**：高风险分类 / 永久删除 / 清空回收站，即使 `--yes` 也要显式授权；
   非交互（管道 / Agent）场景直接拒绝（退出码 4）。
4. **删除前二次防御**：引擎层重新解析路径并复核黑名单，拒绝磁盘根、系统关键文件与受保护路径。
5. **不跟随链接**：符号链接 / junction 一律不删、不清空（含 CLEAR 目标本身是链接的情况）。
6. **MCP 默认只读**：`--mcp` 不暴露写能力；`delete` 需 `--mcp-allow-delete` + 两阶段
   `confirm_token` + 清单漂移检测，危险清单还要 `acknowledge_danger=true`。
7. **不清理注册表**（v0.9.9）：只读扫描报告，CLI 与 MCP 均无清理入口。

## 明确的设计边界（不是漏洞）

- **不清理注册表**：本工具只**只读扫描**并报告可疑项（`--registry-scan` /
  MCP `registry_scan`），**绝不删除任何注册表项**——CLI 与 MCP 层都没有清理入口。
  原因：删除注册表项**不释放磁盘空间**（hive 不会因此变小），而"这条记录还有没有用"
  无法可靠判定，误删会导致软件异常甚至系统问题。v0.9.9 实现该扫描时的实测也印证了
  这一点：MSI 卸载串、``InstallLocation`` 为空、便携软件等都会看起来像"残留"，
  最初基于"压缩产品码匹配"的判定把 41 条 MSI 表项全部误报成残留（内含仍在使用的
  VC++/Node.js/Java 运行时），最终改为调用 Windows Installer API
  （``MsiQueryProductStateW``）做**权威判定**，拿不到结论就不报。
  动手清理请自行核对后用 ``regedit``，并先 ``reg export`` 备份。
- **不清理注册表（续）**：``UserAssist`` / ``Shellbags`` / ``BagMRU`` 等取证类键
  明确不扫——它们"看起来是垃圾"但涉及系统外壳行为，误删会造成资源管理器异常。
- **失效快捷方式只清"确定失效"的**：``broken_shortcuts`` 分类只处理
  "目标所在卷可访问、但目标确实不存在"的 ``.lnk``；离线盘、未插入的移动盘、
  断开的网络共享（``UNAVAILABLE``）、URL 快捷方式、系统命名空间（文件资源管理器 /
  控制面板 / 回收站，``UNKNOWN``）与 0 字节 UWP 占位符（``INVALID``）一律不动。
  开始菜单里的 ``Startup``（开机启动）目录默认排除。
- **不删除系统组件**：`WinSxS` / `DriverStore` / `Windows\Installer` / `$Recycle.Bin` /
  `System Volume Information` 均在保护黑名单内，仅报告体积与官方清理命令。
- **`--shred` 在 SSD/NVMe 上不保证不可恢复**：磨损均衡与 TRIM 会让覆写不可靠；
  需要真正的数据销毁请使用全盘加密（BitLocker）或厂商安全擦除。
- **不做遥测、不上传数据**：工具不联网（`--health` 的查询全部是本地命令 / 注册表读取）。

## 上报漏洞

请**不要**用公开 Issue 披露未修复的安全问题。优先使用 GitHub 的
**Private vulnerability reporting / Security Advisory**（仓库 Security 标签页），
或通过仓库 Issue 说明"需要私下沟通"。

请在报告中包含：

- 影响的版本与命令（例如 `python -m pc_cleaner --json --clean <分类> --yes`）；
- 可复现步骤（**请勿在真实数据上复现**，用临时目录）；
- 期望行为 vs 实际行为；
- 如涉及误删：被删除对象的类型、路径形态（是否含 junction / `..` / `\\?\` / 8.3 短名）。

## 已修复的历史安全问题（示例）

| 版本 | 问题 |
|---|---|
| 0.9.9 | MCP `delete` 不写 history/audit（Agent 删除后无法撤销）；菜单刷新扫描参数错位导致 find_dirs 只遍历 1 层；离线盘/命名空间快捷方式判定（新增能力时的误判，未进入发布版） |
| 0.9.4 | `--json --clean ""` 静默退化为只扫描；MCP `delete` 重扫未带过滤条件导致误报漂移 |
| 0.9.3 | `--json --yes` 绕过二次确认；CLEAR 目标为 junction 时越界清空；`npm-cache` 父规则会删掉正在运行的 npx 工具；路径未规范化导致保护判定不一致 |
| 0.9.2 | 撤销失效、释放量虚报、部分失败静默成功 |

细节见 [CHANGELOG.md](CHANGELOG.md)。
